{
  "name": "personal-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "optimize:media": "node scripts/optimize-media.mjs",
    "prebuild": "npm run optimize:media",
    "build": "node scripts/build-site.mjs",
    "prebuild:static": "npm run optimize:media",
    "build:static": "vite build --outDir dist-static",
    "preview": "vite preview --outDir dist/server/public"
  },
  "devDependencies": {
    "@emnapi/core": "1.11.2",
    "@emnapi/runtime": "1.11.2",
    "sharp": "^0.35.3",
    "vite": "^8.1.5"
  }
}
